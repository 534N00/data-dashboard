import react, { useState } from 'react';

const Sidebar = ({ recipes, setRecipes }) => {
    return(
        <div className="Sidebar">
            <h3>Stats</h3>
            {/* todo: calculate mean among calorie/serving counts */}
        </div>
    );
};

export default Sidebar;